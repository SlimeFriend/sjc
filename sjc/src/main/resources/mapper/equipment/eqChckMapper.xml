<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sjc.app.eq.mapper.EqChckMapper">


	<!-- 전체조회 -->
<select id="selctEqChckAll" resultType="EqChckVO">
	SELECT eq_chck_opcd
	, eq_code
	, eq_nextchck
	, eq_chckox
	, start_date
	, end_date
	, reason
	, comm
	FROM eq_chck
	ORDER BY eq_code
</select>

</mapper>